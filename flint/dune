(library
 (name flint)
 (public_name flint)
 (foreign_archives ((path lib/flint)(mode whole)))
 (foreign_stubs
  (language c)
  (names ocaml_flint_utils)
  (include_dirs
   (lib zarith)))
 (libraries zarith ctypes)
 (flags -w -9-27)
 (ctypes
  (external_library_name flint)
  (build_flags_resolver
   (vendored
    (c_flags
     "-Iflint"
     "-I."
     "-Werror=implicit-function-declaration"
     :standard)
    (c_library_flags
    "-lgmp -lmpfr"

  )))
  (deps lib/flint.h lib/fmpz.h)
  (headers
   (include "lib/fmpz.h" "string.h"))
  (type_description
   (instance Type)
   (functor Type_description))
  (function_description
   (concurrency sequential)
   (instance Function)
   (functor Function_description))
  (generated_types Types_generated)
  (generated_entry_point C)))

;(rule (action (copy %{lib:zarith:zarith.h} zarith.h)))
